name: GitHub Actions Demo
on: 
  push:

jobs:
  job1: 
    name: job1
    runs-on: ubuntu-latest
    container:
      image: alpine:3.15.0
    steps:
      - name: temporary
        run: |
          echo "hogehoge"
          apk list --installed
          echo "fugafuga"
          apk -L info
